import type { FastifyInstance } from "fastify";
import { z } from "zod";
import { deleteAddon, getAddon, listAddons, registerAddon } from "../addons/addon-registry.js";
import { resetAllDocchiEpisodeMappings } from "../docchi/docchi-episode-mapping-store.js";
import { getKometaAnimeIdsStatus, syncKometaAnimeIds } from "../docchi/kometa-anime-ids.js";
import { startManualAnimeSubMissingRetry } from "../libraries/library-automation.js";
import { clearLibraryCache } from "../libraries/library-cache.js";
import { createLibrary, deleteLibrary, getLibrary, listLibraries, updateLibrary } from "../libraries/library-registry.js";
import { EUROPEAN_LANGUAGES } from "../languages/european-languages.js";
import { DOCCHI_KOMETA_ANIME_IDS_REFRESH_INTERVALS, DOCCHI_PUBLIC_MAPPING_MODES, DOCCHI_STREAM_FORCE_MODES, getAppSettings, INTEL_QSV_MODES, LINK_VALIDATION_MODES, METADATA_SYNC_INTERVALS, TRANSCODE_MODES, TRANSCODE_PRESETS, TRANSCODE_QUALITY_ORDER, updateAppSettings, VOD_AUDIO_MODES, VOD_BITRATE_MODES, VOD_BUFFER_PROGRESSION_MODES, VOD_QUALITY_MODES, VOD_TRANSCODE_STRATEGIES } from "../settings/app-settings.js";
import { clearSearchCache, clearSearchHistory, getSearchHistoryDetails, listCachedSearchResults, listSearchHistory } from "../search/search-cache.js";
import { refreshNow } from "../search/cached-selection.js";
import { aggregateStreams } from "../streams/aggregation.js";
import type { StreamType } from "../streams/types.js";
import { fetchAnimeSubSubtitles, isAnimeSubAddon } from "../subtitles/animesub-client.js";
import { clearSubtitleCache, getSubtitleCache, listSubtitleCache, saveSubtitleCache } from "../subtitles/subtitle-cache.js";
import { localizeSubtitleResults } from "../subtitles/subtitle-local-cache.js";
import { registerSubtitlePreviewRoutes } from "../subtitles/subtitle-preview-routes.js";
import { clearSystemLogs, listSystemLogs, type SystemLogLevel, writeSystemLog } from "../system/system-log.js";
import { runTechnicalHealthCheck } from "../system/technical-health.js";
import { fetchTmdbCatalog, fetchTmdbWatchProviders } from "../tmdb/tmdb-client.js";
import { getIntelQsvStatus } from "../transcoding/intel-qsv.js";

const registerAddonSchema = z.object({ manifestUrl: z.string().url(), enabled: z.boolean().optional() });
const aggregateParamsSchema = z.object({ type: z.enum(["movie", "series"]), id: z.string().min(1) });
const limitQuerySchema = z.object({ limit: z.coerce.number().int().positive().max(500).default(50) });
const logsQuerySchema = z.object({ limit: z.coerce.number().int().positive().max(500).default(100), level: z.enum(["debug", "info", "warn", "error"]).optional() });
const automationIntervalSchema = z.preprocess((value) => Number(value), z.union([z.literal(24), z.literal(72), z.literal(168), z.literal(336), z.literal(720)])).optional();
const libraryConfigSchema = z.object({ language: z.string().optional(), region: z.string().optional(), sortBy: z.string().optional(), withGenres: z.string().optional(), withKeywords: z.string().optional(), withOriginalLanguage: z.string().optional(), withWatchProviders: z.string().optional(), watchRegion: z.string().optional(), primaryReleaseDateGte: z.string().optional(), primaryReleaseDateLte: z.string().optional(), firstAirDateGte: z.string().optional(), firstAirDateLte: z.string().optional(), year: z.string().optional(), voteAverageGte: z.coerce.number().min(0).max(10).optional(), voteCountGte: z.coerce.number().int().min(0).optional(), itemLimit: z.coerce.number().int().min(1).max(100).optional(), docchiPublicMappingMode: z.enum(["inherit", ...DOCCHI_PUBLIC_MAPPING_MODES]).optional(), docchiAutomationMode: z.enum(["inherit", ...DOCCHI_PUBLIC_MAPPING_MODES]).optional(), docchiAutomationIntervalHours: automationIntervalSchema, animeSubAutomationMode: z.enum(["manual", "24h", "3d", "7d", "14d", "30d"]).optional(), animeSubAutomationIntervalHours: automationIntervalSchema, animeSubMissingRetryMode: z.enum(["never", "once", "twice", "daily"]).optional(), includeAdult: z.boolean().optional(), timeWindow: z.enum(["day", "week"]).optional() }).partial();
const librarySchema = z.object({ name: z.string().min(1), slug: z.string().optional(), type: z.enum(["movie", "series"]), source: z.literal("tmdb").optional(), mode: z.enum(["discover", "trending", "popular", "top_rated", "now_playing", "upcoming", "airing_today", "on_the_air"]), enabled: z.boolean().optional(), sortOrder: z.coerce.number().int().optional(), config: libraryConfigSchema.optional() });
const settingsSchema = z.object({ preferredAudioLanguage: z.string().min(2).optional(), preferredSubtitleLanguage: z.string().min(2).optional(), preferDebrid: z.boolean().optional(), detectDebridPlaceholders: z.boolean().optional(), debridPlaceholderValidationMode: z.enum(LINK_VALIDATION_MODES).optional(), debridPlaceholderMinSizeMb: z.coerce.number().int().min(1).max(102400).optional(), debridPlaceholderMinDurationMinutes: z.coerce.number().int().min(1).max(1440).optional(), debridPlaceholderCompareDeclaredSize: z.boolean().optional(), debridPlaceholderSizeDifferenceGb: z.coerce.number().int().min(1).max(1024).optional(), defaultTranscodeBufferPreset: z.string().optional(), streamValidationTimeoutMs: z.coerce.number().int().positive().max(120000).optional(), linkValidationMode: z.enum(LINK_VALIDATION_MODES).optional(), maxTranscodeSessions: z.coerce.number().int().positive().max(16).optional(), publicBaseUrl: z.string().url().optional().or(z.literal("")), autoRefreshCache: z.boolean().optional(), showDiagnosticDetails: z.boolean().optional(), tmdbApiKey: z.string().optional(), tmdbReadAccessToken: z.string().optional(), tmdbLanguage: z.string().optional(), tmdbRegion: z.string().optional(), metadataSyncIntervalMinutes: z.coerce.number().refine((value) => (METADATA_SYNC_INTERVALS as readonly number[]).includes(value), { message: "Invalid metadata sync interval." }).optional(), docchiPublicMappingMode: z.enum(DOCCHI_PUBLIC_MAPPING_MODES).optional(), docchiKometaAnimeIdsEnabled: z.boolean().optional(), docchiKometaAnimeIdsRefreshInterval: z.enum(DOCCHI_KOMETA_ANIME_IDS_REFRESH_INTERVALS).optional(), docchiStreamForceMode: z.enum(DOCCHI_STREAM_FORCE_MODES).optional(), autoTranscodeMinQuality: z.enum(TRANSCODE_QUALITY_ORDER).optional(), autoTranscodeMaxQuality: z.enum(TRANSCODE_QUALITY_ORDER).optional(), transcodePreset: z.enum(TRANSCODE_PRESETS).optional(), transcodeCrfMode: z.enum(["auto", "range"]).optional(), transcodeCrfMin: z.coerce.number().int().min(16).max(35).optional(), transcodeCrfMax: z.coerce.number().int().min(16).max(35).optional(), transcodeBitrateMode: z.enum(["auto", "range"]).optional(), transcodeBitrateMinKbps: z.coerce.number().int().min(150).max(50000).optional(), transcodeBitrateMaxKbps: z.coerce.number().int().min(150).max(50000).optional(), transcodeMode: z.enum(TRANSCODE_MODES).optional(), liveIntelQsvMode: z.enum(INTEL_QSV_MODES).optional(), vodIntelQsvMode: z.enum(INTEL_QSV_MODES).optional(), vodTranscodeStrategy: z.enum(VOD_TRANSCODE_STRATEGIES).optional(), vodSegmentSeconds: z.coerce.number().int().min(2).max(30).optional(), vodStartupBufferSeconds: z.coerce.number().int().min(0).max(600).optional(), vodBufferProgression: z.enum(VOD_BUFFER_PROGRESSION_MODES).optional(), vodAdaptiveBatchEnabled: z.boolean().optional(), vodFixedBatchSegmentCount: z.coerce.number().int().min(1).max(100).optional(), vodQualityMode: z.enum(VOD_QUALITY_MODES).optional(), vodCrf: z.coerce.number().int().min(16).max(35).optional(), vodBitrateMode: z.enum(VOD_BITRATE_MODES).optional(), vodAudioMode: z.enum(VOD_AUDIO_MODES).optional() });

export async function registerAdminRoutes(app: FastifyInstance): Promise<void> {
  await registerSubtitlePreviewRoutes(app);
  app.get("/admin/settings", async () => ({ settings: getAppSettings() }));
  app.get("/admin/transcode/qsv/status", async () => { const status = getIntelQsvStatus(true); writeSystemLog(status.enabled ? "info" : "warn", "transcode", status.enabled ? "Intel QSV status check succeeded." : "Intel QSV status check failed.", status); return { status }; });
  app.get("/admin/docchi/status", async () => { const addons = listAddons(); const matches = addons.filter(isDocchiAddon); const enabled = matches.filter((addon) => addon.enabled && addon.status === "online"); return { detected: matches.length > 0, enabled: enabled.length > 0, addons: matches.map((addon) => ({ id: addon.id, name: addon.name, manifestUrl: addon.manifestUrl, enabled: addon.enabled, status: addon.status })) }; });
  app.get("/admin/animesub/status", async () => { const addons = listAddons(); const matches = addons.filter(isAnimeSubAddon); const enabled = matches.filter((addon) => addon.enabled && addon.status === "online" && addon.supportedResources.includes("subtitles")); return { detected: matches.length > 0, enabled: enabled.length > 0, addons: matches.map((addon) => ({ id: addon.id, name: addon.name, manifestUrl: addon.manifestUrl, enabled: addon.enabled, status: addon.status, supportedResources: addon.supportedResources })) }; });
  app.get<{ Params: { type: StreamType; id: string } }>("/admin/animesub/:type/:id", async (request, reply) => { const params = aggregateParamsSchema.safeParse(request.params); if (!params.success) { reply.code(400); return { error: "Invalid AnimeSub subtitle parameters.", details: params.error.flatten() }; } return { cache: getSubtitleCache(params.data.type, params.data.id) ?? null }; });
  app.post<{ Params: { type: StreamType; id: string } }>("/admin/animesub/:type/:id/fetch", async (request, reply) => { const params = aggregateParamsSchema.safeParse(request.params); if (!params.success) { reply.code(400); return { error: "Invalid AnimeSub subtitle parameters.", details: params.error.flatten() }; } writeSystemLog("info", "animesub", "Manual AnimeSub subtitle fetch requested.", params.data); const cache = await fetchAndSaveAnimeSub(params.data.type, params.data.id); return { cache }; });
  app.get<{ Querystring: { limit?: string } }>("/admin/animesub/cache", async (request, reply) => { const query = limitQuerySchema.safeParse(request.query); if (!query.success) { reply.code(400); return { error: "Invalid subtitle cache query.", details: query.error.flatten() }; } return { cache: listSubtitleCache(query.data.limit) }; });
  app.delete("/admin/animesub/cache", async () => ({ ok: true, deleted: clearSubtitleCache() }));
  app.get("/admin/docchi/kometa/status", async () => ({ kometa: getKometaAnimeIdsStatus() }));
  app.post("/admin/docchi/kometa/sync", async () => ({ kometa: await syncKometaAnimeIds(true) }));
  app.post("/admin/docchi/restore-original-indexes", async () => ({ ok: true, reset: resetAllDocchiEpisodeMappings() }));
  app.get("/admin/languages", async () => { const languages = [...EUROPEAN_LANGUAGES].sort((a, b) => { if (a.code === "pl") return -1; if (b.code === "pl") return 1; if (a.code === "en") return -1; if (b.code === "en") return 1; return a.nativeName.localeCompare(b.nativeName); }); return { languages: languages.map((language) => ({ code: language.code, iso6392: language.iso6392, englishName: language.englishName, nativeName: language.nativeName, label: `${language.nativeName} / ${language.englishName}` })) }; });
  app.patch("/admin/settings", async (request, reply) => { const body = settingsSchema.safeParse(request.body); if (!body.success) { reply.code(400); return { error: "Invalid settings payload.", details: body.error.flatten() }; } const settings = updateAppSettings(body.data); const kometa = body.data.docchiKometaAnimeIdsEnabled === true ? await syncKometaAnimeIds(false) : getKometaAnimeIdsStatus(); writeSystemLog("info", "settings", "Admin settings updated.", { changedKeys: Object.keys(body.data), kometa }); return { settings, kometa }; });
  app.get<{ Params: { type: "movie" | "series" } }>("/admin/tmdb/watch-providers/:type", async (request, reply) => { const params = z.object({ type: z.enum(["movie", "series"]) }).safeParse(request.params); if (!params.success) { reply.code(400); return { error: "Invalid provider type." }; } return { providers: await fetchTmdbWatchProviders(params.data.type) }; });
  app.get("/admin/libraries", async () => ({ libraries: listLibraries() }));
  app.post("/admin/libraries", async (request, reply) => { const body = librarySchema.safeParse(request.body); if (!body.success) { reply.code(400); return { error: "Invalid library payload.", details: body.error.flatten() }; } const library = createLibrary(body.data); writeSystemLog("info", "libraries", "Library created.", { id: library.id, name: library.name, type: library.type, mode: library.mode }); reply.code(201); return { library }; });
  app.patch("/admin/libraries/:id", async (request, reply) => { const body = librarySchema.partial().safeParse(request.body); if (!body.success) { reply.code(400); return { error: "Invalid library update payload.", details: body.error.flatten() }; } const id = (request.params as { id: string }).id; const library = updateLibrary(id, body.data); if (!library) { reply.code(404); return { error: "Library not found." }; } clearLibraryCache(library.id); writeSystemLog("info", "libraries", "Library updated.", { id: library.id, name: library.name }); return { library }; });
  app.post("/admin/libraries/:id/test", async (request, reply) => { const id = (request.params as { id: string }).id; const library = getLibrary(id); if (!library) { reply.code(404); return { error: "Library not found." }; } const metas = await fetchTmdbCatalog(library, 1); return { library, metas: metas.slice(0, 10) }; });
  app.post("/admin/libraries/:id/refresh", async (request, reply) => { const id = (request.params as { id: string }).id; const library = getLibrary(id); if (!library) { reply.code(404); return { error: "Library not found." }; } const deleted = clearLibraryCache(library.id); return { ok: true, deleted }; });
  app.post("/admin/libraries/:id/animesub/retry-missing", async (request, reply) => { const id = (request.params as { id: string }).id; const library = getLibrary(id); if (!library) { reply.code(404); return { error: "Library not found." }; } const started = startManualAnimeSubMissingRetry(library); if (!started.started) reply.code(409); return started; });
  app.delete("/admin/libraries/:id", async (request, reply) => { const id = (request.params as { id: string }).id; const deleted = deleteLibrary(id); if (!deleted) { reply.code(404); return { error: "Library not found." }; } writeSystemLog("info", "libraries", "Library deleted.", { id }); return { ok: true }; });
  app.get("/admin/system/health", async () => { const report = await runTechnicalHealthCheck(); writeSystemLog(report.status === "error" ? "error" : report.status === "warn" ? "warn" : "info", "health", "Technical health-check completed.", { status: report.status }); return { report }; });
  app.get<{ Querystring: { limit?: string; level?: SystemLogLevel } }>("/admin/system/logs", async (request, reply) => { const query = logsQuerySchema.safeParse(request.query); if (!query.success) { reply.code(400); return { error: "Invalid logs query.", details: query.error.flatten() }; } return { logs: listSystemLogs(query.data.limit, query.data.level) }; });
  app.delete("/admin/system/logs", async () => { clearSystemLogs(); writeSystemLog("info", "logs", "System logs cleared."); return { ok: true }; });
  app.get("/admin/addons", async () => ({ addons: listAddons().map(toAdminAddon) }));
  app.post("/admin/addons", async (request, reply) => { const body = registerAddonSchema.safeParse(request.body); if (!body.success) { reply.code(400); return { error: "Invalid addon registration payload.", details: body.error.flatten() }; } const addon = await registerAddon(body.data); writeSystemLog(addon.status === "online" ? "info" : "warn", "addons", "Addon registered.", { id: addon.id, name: addon.name, manifestUrl: addon.manifestUrl, status: addon.status, lastError: addon.lastError, detectedAnimeSub: isAnimeSubAddon(addon) }); reply.code(201); return { addon: toAdminAddon(addon) }; });
  app.get<{ Params: { type: "movie" | "series"; id: string } }>("/admin/aggregate/:type/:id", async (request, reply) => { const params = aggregateParamsSchema.safeParse(request.params); if (!params.success) { reply.code(400); return { error: "Invalid aggregation parameters.", details: params.error.flatten() }; } writeSystemLog("info", "diagnostics", "Aggregation diagnostics started.", params.data); const result = await aggregateStreams(params.data.type, params.data.id); return result; });
  app.get("/admin/cache", async (request, reply) => { const query = limitQuerySchema.safeParse(request.query); if (!query.success) { reply.code(400); return { error: "Invalid cache query.", details: query.error.flatten() }; } return { cache: listCachedSearchResults(query.data.limit) }; });
  app.delete("/admin/cache", async () => ({ ok: true, deleted: clearSearchCache() }));
  app.post<{ Params: { type: StreamType; id: string } }>("/admin/cache/:type/:id/refresh", async (request, reply) => { const params = aggregateParamsSchema.safeParse(request.params); if (!params.success) { reply.code(400); return { error: "Invalid cache refresh parameters.", details: params.error.flatten() }; } const result = await refreshNow(params.data.type, params.data.id); return { result }; });
  app.get("/admin/history", async (request, reply) => { const query = limitQuerySchema.safeParse(request.query); if (!query.success) { reply.code(400); return { error: "Invalid history query.", details: query.error.flatten() }; } return { history: listSearchHistory(query.data.limit) }; });
  app.delete("/admin/history", async () => ({ ok: true, deleted: clearSearchHistory() }));
  app.get<{ Params: { id: string } }>("/admin/history/:id", async (request) => ({ details: getSearchHistoryDetails(request.params.id) }));
}

async function fetchAndSaveAnimeSub(type: StreamType, id: string) { const subtitles = await fetchAnimeSubSubtitles(type, id); const localized = await localizeSubtitleResults(type, id, subtitles).catch(() => subtitles); return saveSubtitleCache(type, id, localized); }
function isDocchiAddon(addon: { manifestUrl: string; name?: string; detectedDocchi?: boolean }): boolean { return Boolean(addon.detectedDocchi || /docchi/i.test(`${addon.name ?? ""} ${addon.manifestUrl}`)); }
function toAdminAddon(addon: ReturnType<typeof getAddon> extends infer T ? NonNullable<T> : never) { return addon; }
